import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meutreino.app',
  appName: 'Meu Treino',
  webDir: 'dist',
  android: {
    path: 'android'
  }
};

export default config;
